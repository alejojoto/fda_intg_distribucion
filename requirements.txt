# Versiones FIJADAS (2026-07-29): el lanzador hace `pip install` en cada
# arranque en las máquinas de operación, así que una versión nueva de una
# dependencia podría romper una máquina sin que cambie el código propio.
# Para subir una versión: probarla primero en el Mac, cambiar el número aquí
# y commitear — el cambio de este archivo es lo que dispara la reinstalación
# allá. Las dependencias transitivas quedan libres a propósito (fijarlas
# exigiría herramientas de compilación de requirements); si alguna rompe,
# se fija aquí mismo con su ==.
supabase==2.31.0
python-dotenv==1.2.2

# Lectura de la hoja de Google Sheets de pedidos en KG (cuenta de servicio,
# solo lectura — autorizada por el usuario 2026-07-20)
gspread==6.2.1

# Panel de operación (Fase 4)
flask==3.1.3

# La ventana propia del panel (pywebview) NO va aquí: es opcional y vive en
# requirements-ventana.txt. Aquí solo entra lo que el asistente necesita para
# funcionar, porque si una de estas líneas falla al instalar se cae el
# `pip install` entero y ninguna se actualiza.

# SOLO instalar en la máquina Windows (automatización UI de Oportunidad de
# Negocios, Fase 3). No funciona ni hace falta en Mac/Linux:
# pywinauto==0.6.9
